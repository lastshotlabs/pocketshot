import { createPocketshot } from '@lastshotlabs/pocketshot'

export const pocketshot = createPocketshot({
  apiUrl: 'https://api.example.com', // placeholder — showcase does not hit real APIs
  tokenKey: 'showcase_token',
})
