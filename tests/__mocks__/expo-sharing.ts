// Stub for expo-sharing — replaced by vi.mock() in tests that need assertions.
export const isAvailableAsync = () => Promise.resolve(true)
export const shareAsync = (_url: string, _opts?: unknown) => Promise.resolve()
