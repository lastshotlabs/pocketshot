# Pocketshot Component Classification

> Generated 2026-04-14 during shared frontend contract execution. This is the working Phase 5 classification baseline keyed off the current checked-in component inventories in Pocketshot and Snapshot.

## Classification Rules

- `shared`: the component has a current Snapshot counterpart by canonical directory name and should converge on one shared contract surface in `@lastshotlabs/frontend-contract`.
- `native-only`: the component has no current Snapshot counterpart and remains Pocketshot-owned until a cross-platform concept is defined.
- `delete`: reserved for duplicate aliases; none are marked delete in this first execution pass because the current Pocketshot registry does not expose obvious parallel aliases.

| Component Path                                       | Current Exported Name    | Canonical Shared Name Or `native-only` | Target Bucket | Target Schema Source                                                                   | Target Slot/State Source                                                                 | Notes                                                                  |
| ---------------------------------------------------- | ------------------------ | -------------------------------------- | ------------- | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| `src/ui/components/auth/forgot-password-form`        | `ForgotPasswordForm`     | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/auth/login-form`                  | `LoginForm`              | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/auth/register-form`               | `RegisterForm`           | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/commerce/cart-item`               | `CartItem`               | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/commerce/price-display`           | `PriceDisplay`           | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/commerce/pricing-table`           | `PricingTable`           | `PricingTable`                         | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/commerce/product-card`            | `ProductCard`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/communication/activity-feed`      | `ActivityFeed`           | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/communication/chat-bubble`        | `ChatBubble`             | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/communication/chat-window`        | `ChatWindow`             | `ChatWindow`                           | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/communication/comment-section`    | `CommentSection`         | `CommentSection`                       | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/communication/emoji-picker`       | `EmojiPicker`            | `EmojiPicker`                          | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/communication/feed`               | `Feed`                   | `Feed`                                 | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/communication/gif-picker`         | `GifPicker`              | `GifPicker`                            | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/communication/message-thread`     | `MessageThread`          | `MessageThread`                        | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/communication/notification-item`  | `NotificationItem`       | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/communication/presence-indicator` | `PresenceIndicator`      | `PresenceIndicator`                    | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/communication/reaction-bar`       | `ReactionBar`            | `ReactionBar`                          | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/communication/reaction-picker`    | `ReactionPicker`         | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/communication/typing-indicator`   | `TypingIndicator`        | `TypingIndicator`                      | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/audio-player`             | `AudioPlayer`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/content/body`                     | `Body`                   | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/content/code-block`               | `CodeBlock`              | `CodeBlock`                            | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/compare-view`             | `CompareView`            | `CompareView`                          | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/file-uploader`            | `FileUploader`           | `FileUploader`                         | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/heading`                  | `Heading`                | `Heading`                              | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/image`                    | `Image`                  | `Image`                                | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/image-viewer`             | `ImageViewer`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/content/label`                    | `Label`                  | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/content/link`                     | `Link`                   | `Link`                                 | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/link-embed`               | `LinkEmbed`              | `LinkEmbed`                            | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/markdown`                 | `Markdown`               | `Markdown`                             | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/media-picker`             | `MediaPicker`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/content/qr-code`                  | `QrCode`                 | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/content/qr-scanner`               | `QrScanner`              | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/content/rich-input`               | `RichInput`              | `RichInput`                            | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/rich-text-editor`         | `RichTextEditor`         | `RichTextEditor`                       | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/content/rich-text-viewer`         | `RichTextViewer`         | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/content/video-player`             | `VideoPlayer`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/data/alert`                       | `Alert`                  | `Alert`                                | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/avatar`                      | `Avatar`                 | `Avatar`                               | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/avatar-group`                | `AvatarGroup`            | `AvatarGroup`                          | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/badge`                       | `Badge`                  | `Badge`                                | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/chart`                       | `Chart`                  | `Chart`                                | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/data-list`                   | `DataList`               | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/data/data-table`                  | `DataTable`              | `DataTable`                            | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/detail-card`                 | `DetailCard`             | `DetailCard`                           | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/empty-state`                 | `EmptyState`             | `EmptyState`                           | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/entity-picker`               | `EntityPicker`           | `EntityPicker`                         | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/favorite-button`             | `FavoriteButton`         | `FavoriteButton`                       | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/filter-bar`                  | `FilterBar`              | `FilterBar`                            | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/filter-sheet`                | `FilterSheet`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/data/highlighted-text`            | `HighlightedText`        | `HighlightedText`                      | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/loading-state`               | `LoadingState`           | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/data/notification-bell`           | `NotificationBell`       | `NotificationBell`                     | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/pagination`                  | `Pagination`             | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/data/progress-circle`             | `ProgressCircle`         | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/data/pull-to-refresh`             | `PullToRefresh`          | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/data/save-indicator`              | `SaveIndicator`          | `SaveIndicator`                        | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/skeleton`                    | `Skeleton`               | `Skeleton`                             | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/sort-picker`                 | `SortPicker`             | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/data/stat-card`                   | `StatCard`               | `StatCard`                             | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/data/tooltip`                     | `Tooltip`                | `Tooltip`                              | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/auto-form`                  | `AutoForm`               | `AutoForm`                             | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/button`                     | `Button`                 | `Button`                               | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/checkbox`                   | `Checkbox`               | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/checkbox-group`             | `CheckboxGroup`          | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/date-picker`                | `DatePicker`             | `DatePicker`                           | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/date-range-picker`          | `DateRangePicker`        | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/form-field`                 | `FormField`              | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/inline-edit`                | `InlineEdit`             | `InlineEdit`                           | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/location-input`             | `LocationInput`          | `LocationInput`                        | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/multi-select`               | `MultiSelect`            | `MultiSelect`                          | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/password-input`             | `PasswordInput`          | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/phone-input`                | `PhoneInput`             | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/pin-input`                  | `PinInput`               | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/quick-add`                  | `QuickAdd`               | `QuickAdd`                             | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/radio-group`                | `RadioGroup`             | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/rating-input`               | `RatingInput`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/search-bar`                 | `SearchBar`              | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/select`                     | `Select`                 | `Select`                               | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/slider`                     | `Slider`                 | `Slider`                               | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/switch`                     | `Switch`                 | `Switch`                               | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/tag-selector`               | `TagSelector`            | `TagSelector`                          | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/textarea`                   | `Textarea`               | `Textarea`                             | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/text-input`                 | `TextInput`              | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/time-picker`                | `TimePicker`             | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/forms/toggle`                     | `Toggle`                 | `Toggle`                               | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/forms/wizard`                     | `Wizard`                 | `Wizard`                               | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/layout/card`                      | `Card`                   | `Card`                                 | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/layout/divider`                   | `Divider`                | `Divider`                              | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/layout/keyboard-avoiding-screen`  | `KeyboardAvoidingScreen` | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/layout/row`                       | `Row`                    | `Row`                                  | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/layout/screen`                    | `Screen`                 | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/layout/scroll-container`          | `ScrollContainer`        | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/layout/section`                   | `Section`                | `Section`                              | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/layout/spacer`                    | `Spacer`                 | `Spacer`                               | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/layout/stack`                     | `Stack`                  | `Stack`                                | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/navigation/accordion`             | `Accordion`              | `Accordion`                            | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/navigation/back-button`           | `BackButton`             | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/navigation/bottom-tab-bar`        | `BottomTabBar`           | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/navigation/drawer-menu`           | `DrawerMenu`             | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/navigation/header`                | `Header`                 | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/navigation/segmented-control`     | `SegmentedControl`       | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/navigation/tabs`                  | `Tabs`                   | `Tabs`                                 | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/navigation/top-bar`               | `TopBar`                 | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/navigation/tree-view`             | `TreeView`               | `TreeView`                             | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/overlay/action-sheet`             | `ActionSheet`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/overlay/bottom-sheet`             | `BottomSheet`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/overlay/command-palette`          | `CommandPalette`         | `CommandPalette`                       | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/overlay/confirm-dialog`           | `ConfirmDialog`          | `ConfirmDialog`                        | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/overlay/context-menu`             | `ContextMenu`            | `ContextMenu`                          | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/overlay/drawer`                   | `Drawer`                 | `Drawer`                               | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/overlay/dropdown-menu`            | `DropdownMenu`           | `DropdownMenu`                         | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/overlay/modal`                    | `Modal`                  | `Modal`                                | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/overlay/popover`                  | `Popover`                | `Popover`                              | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/overlay/toast`                    | `Toast`                  | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/workflow/audit-log`               | `AuditLog`               | `AuditLog`                             | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/workflow/calendar`                | `Calendar`               | `Calendar`                             | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/workflow/kanban-board`            | `KanbanBoard`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/workflow/notification-feed`       | `NotificationFeed`       | `NotificationFeed`                     | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/workflow/progress-bar`            | `ProgressBar`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/workflow/status-badge`            | `StatusBadge`            | `native-only`                          | `native-only` | Pocketshot local schema until a shared concept exists                                  | Pocketshot native surface/runtime definitions                                            | No current Snapshot component with the same canonical directory name.  |
| `src/ui/components/workflow/stepper`                 | `Stepper`                | `Stepper`                              | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
| `src/ui/components/workflow/timeline`                | `Timeline`               | `Timeline`                             | `shared`      | `@lastshotlabs/frontend-contract` component contract + Pocketshot native props wrapper | Shared universal slot/state contract projected through Pocketshot native surface runtime | Snapshot inventory already contains the same canonical component name. |
